# Environment Variables

## Client-side

Prefix: `VITE_`
Defined in: `src/env.ts` via T3 Env

**Required:**
- `VITE_CLERK_PUBLISHABLE_KEY`
- `VITE_CONVEX_URL`

## Convex (Server)

Set via: `bunx convex env set KEY=value`

**Required:**
- `CLERK_WEBHOOK_SECRET`
- `GOOGLE_MAPS_API_KEY`
