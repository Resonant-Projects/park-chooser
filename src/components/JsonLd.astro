---
/* eslint-disable astro/no-set-html-directive -- JSON.stringify safely escapes content */
interface Props {
  schema: Record<string, unknown>;
}

const { schema } = Astro.props;
---

<script type="application/ld+json" set:html={JSON.stringify(schema)} />
